# 宿主机发布的应用端口（验证环境由 CC_PUBLISH_PORT 注入）
CC_PUBLISH_PORT=3025

# 数据库口令（仅 compose 内部网络使用，不发布到宿主）
DB_PASSWORD=nursery123

# JWT 签名密钥（生产环境务必更换为足够长的随机串）
JWT_SECRET=nursery-default-secret-key-0123456789abcdef0123456789abcdef
